export enum MotorCondition {
  NEW = "new",
  USED = "used",
}

export enum MotorFuelType {
  GASOLINE = "gasoline",
  ELECTRICITY = "electricity",
}

export enum MotorTransmission {
  AUTOMATIC = "automatic",
  SEMI_AUTOMATIC = "semi-automatic",
}
